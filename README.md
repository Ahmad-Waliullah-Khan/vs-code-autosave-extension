# auto-save README

VS Code extension to save files automatically

# To run the extension:

- cd into the directory
- npm install
- open the repository in vscode
- press F5 to run the extension
- to activate the feature, press cmd+shift+p and select Enable Auto Save
- A message should show up saying the feature has been activated
